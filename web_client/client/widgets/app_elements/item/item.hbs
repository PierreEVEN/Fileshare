<div class="item-preview" oncontextmenu="{{ctx "context_menu(event)"}}" onpointerdown="{{ctx "click(event)"}}" ondblclick="{{ctx "dblclick()"}}">
    <div class="item_thumbnail">
        {{item_thumbnail item}}
    </div>
    <div class="title">
        <p>{{item.name}}</p>
    </div>
</div>